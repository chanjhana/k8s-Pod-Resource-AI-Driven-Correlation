apiVersion: v1
kind: Service
metadata:
  name: edgemind-server-svc
  namespace: monitoring
  labels:
    app: edgemind-server
spec:
  type: NodePort
  selector:
    app: edgemind-server
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
