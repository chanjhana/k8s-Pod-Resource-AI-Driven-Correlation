# InfluxDB auth token.
# edgemind-dev-token matches the token set in the InfluxDB Helm install.
# In production this would be a proper secret manager reference.
apiVersion: v1
kind: Secret
metadata:
  name: influxdb-token
  namespace: pump-station
type: Opaque
stringData:
  token: "edgemind-dev-token"
